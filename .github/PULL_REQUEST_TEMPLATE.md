## Jira and implementation

- Jira key:
- Starting `dev` SHA and linked dependencies:
- Feature switch (OFF by default), ON prerequisites, and rollback:
- Migrations (ID, additive compatibility, rollback or `none`):

## Verification

- Local commands and results:
- CI frontend and backend results (link; `pending` until complete):
- Negative, permission, payment, time-zone and concurrent cases exercised:
- Manual DEV / provider / device checks that still need evidence:

## After merge handoff

- Merged `dev` SHA and DEV deployment link/time:
- Effective DEV toggle values, migration version, screenshots/API evidence:
- Risks, outstanding QA, Jira status, and next story key:
